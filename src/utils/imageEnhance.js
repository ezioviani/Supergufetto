export async function enhanceImage(file){const img=new Image();const url=URL.createObjectURL(file);return new Promise((resolve)=>{img.onload=()=>{const c=document.createElement('canvas');const x=c.getContext('2d');c.width=img.width;c.height=img.height;x.filter='grayscale(100%) contrast(180%) brightness(120%)';x.drawImage(img,0,0);c.toBlob((b)=>resolve(b),'image/jpeg',0.95)};img.src=url})}